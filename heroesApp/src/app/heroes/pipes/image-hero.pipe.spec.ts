import { ImageHeroPipe } from './image-hero.pipe';

describe('ImageHeroPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageHeroPipe();
    expect(pipe).toBeTruthy();
  });
});
